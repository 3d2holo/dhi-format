# JavaScript Libraries

This directory contains the JavaScript libraries for `dhi-format`.

## Structure

- `encoder`: JavaScript encoder library for building `dhi-format` payloads
- `decoder`: JavaScript decoder library for parsing `dhi-format` payloads

## Notes

The current implementation provides a clean project skeleton and a minimal API surface for future binary-format work.
